- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.config/ranger: 'config/ranger'
    ~/.config/termite: 'config/termite'
    ~/.local/share/icons: 'local/share/icons'
